version: "3"
services:
  pail:
    container_name: pail
    image: kittyandrew/pail:latest
    restart: always
    # Expose port for direct access (remove if using a reverse proxy on the same Docker network)
    ports:
      - "8080:8080"
    volumes:
      - ./config.toml:/etc/pail/config.toml:ro
      - ./data/pail:/var/lib/pail
      - ./data/opencode-auth:/root/.local/share/opencode
      - ./data/opencode-config:/root/.config/opencode
    # Option A: pass API keys as env vars (no opencode auth needed)
    # environment:
    #   - OPENAI_API_KEY=${OPENAI_API_KEY}
    #   - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}

# Uncomment to join an external Docker network (e.g., for a Caddy reverse proxy).
# With this, remove the `ports` section above — Caddy connects via the network name.
# networks:
#   default:
#     name: caddynet
#     external: true
